# Recipe Book Project

## Setup

1. Copy `.env.example` to `.env` in both frontend and backend folders.
2. Fill in real values when you create database/accounts.
3. Install dependencies:
   - `npm install` (in root, frontend/, backend/)
4. Run backend: `cd backend && npm run start:dev`
5. Run frontend: `cd frontend && npm run dev`
